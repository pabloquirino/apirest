

services:
  postgres:
    image: postgres:16-alpine
    container_name: apirest_db
    environment:
      POSTGRES_USER:     ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB:       ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - apirest_net

  pgadmin:
    image: dpage/pgadmin4
    container_name: apirest_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL:    admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - apirest_net

volumes:
  postgres_data:

networks:
  apirest_net:
    driver: bridge